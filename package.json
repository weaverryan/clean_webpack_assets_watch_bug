{
  "devDependencies": {
    "@symfony/webpack-encore": "^0.30.1"
  },
  "scripts": {
      "dev": "encore dev",
      "watch": "encore dev --watch",
      "build": "encore production --progress"
  }
}
